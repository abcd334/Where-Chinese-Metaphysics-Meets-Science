# Demo screenshots

- `streamlit-relations.jpg`: detected stem/branch relationships for the canonical example.
- `streamlit-trace.jpg`: expanded 壬 × 乙 → 傷官 trace, including rule ID and validation status.

Captured on 2026-09-25 from the actual local Streamlit app, using its default input:
`丙寅 辛卯 壬戌 乙巳`. These are unmodified viewport screenshots, not mockups.

To refresh: start the app using [the demo instructions](../demo.md), press Analyze,
capture the Detected Relationships section, then expand the hour-stem reasoning section and capture it.
Only the built-in example is shown; no personal birth data is used.
